services:
  safetylens:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DB_PATH=/data/safetylens.db
    volumes:
      - ./backend/snapshots:/app/backend/snapshots
      - safetylens_data:/data
    restart: unless-stopped

volumes:
  safetylens_data:
